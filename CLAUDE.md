# Claude Code Instructions

Before doing any work in this repository, read [.agents/AGENTS.md](.agents/AGENTS.md) for project rules (build system, build parallelism/memory limits, notifications, deployment). Follow it exactly — in particular the build parallelism limits, which exist to prevent out-of-memory crashes on the build host.
