module;

#include "quickjs_host.hpp"

export module rouen.hosts.quickjs_host;

export namespace rouen::hosts {
    using rouen::hosts::quickjs_host;
    using rouen::hosts::quickjs_max_memory_bytes;
    using rouen::hosts::quickjs_script_timeout;
}
