module;

#include "telegram_host.hpp"

export module rouen.hosts.telegram_host;

export namespace rouen::hosts {
    using rouen::hosts::telegram_host;
    using rouen::hosts::telegram_chat_session;
    using rouen::hosts::telegram_message;
    using rouen::hosts::telegram_route;
}
